import * as React from 'react';
import '../../../styles/utils.scss';

const Loading = () => {
  return (
    <div>
      <div className="contentWrapper headTitle">Loading...</div>
    </div>
  );
};

export default Loading;
