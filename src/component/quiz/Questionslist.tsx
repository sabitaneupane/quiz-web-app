import * as React from 'react';

const Questionslist = (props) => {
    const {questionsList} = props;
	return (
        <div>
            {questionsList}
        </div>
    );	
}

export default Questionslist;


